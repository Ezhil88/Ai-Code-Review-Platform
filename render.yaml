services:
  - type: web
    name: ai-code-review-platform
    runtime: node
    rootDir: ai-code-review
    plan: free
    buildCommand: cd frontend && npm ci --legacy-peer-deps && npm run build && cd ../backend && npm ci
    startCommand: cd backend && npm start
    healthCheckPath: /health
    envVars:
      - key: NPM_CONFIG_LEGACY_PEER_DEPS
        value: "true"
      - key: NODE_VERSION
        value: 20
